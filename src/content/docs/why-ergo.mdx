---
title: Why ergo?
description: The design philosophy behind ergo and why the Fast Fail approach produces better REST APIs.
---

## The Name

**ergo** is a play on _"error or go"_ — every middleware either rejects the
request with a proper error or passes it forward. There is no ambiguity, no
silent swallowing, no deferred validation. The name also carries its Latin
meaning: _therefore_ — as in, "the request is valid; _therefore_, proceed."

## API Naming

ergo's public API names are deliberate architectural choices, not arbitrary
labels. Each was evaluated against ecosystem alternatives and retained for
accuracy. The full audit is documented in ergo's internal decision record
([#139](https://github.com/CentralPing/ergo/issues/139)).

### `send`

A factory that returns the post-pipeline response writer — called once by
`handler()` after the pipeline completes. It serializes the accumulated
response (JSON, ETags, conditionals, [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) errors, pagination).

Unlike Express's `res.send()` or Fastify's `reply.send()`, ergo's `send` is
not a method called inside a route handler. It operates at a different
architectural level — a factory wired by the framework, not an imperative call
in user code.

**Rejected:** `respond` (semantically vague), `reply` (Fastify's term with
different invocation semantics), `complete` (too generic).

### `accepts`

A pipeline middleware for content negotiation — validates the client's `Accept`,
`Accept-Language`, `Accept-Charset`, and `Accept-Encoding` headers per
[RFC 9110 §12.5](https://www.rfc-editor.org/rfc/rfc9110#section-12.5).

The name is a verb form of the HTTP header name. It maps directly to the
protocol concept rather than inventing a framework-specific abstraction.

**Rejected:** `negotiate` (overloaded — could mean auth or protocol
negotiation), `contentType` (covers only one of the four Accept headers).

### `compose`

Left-to-right two-accumulator pipeline composition. `compose` is the standard
term for combining functions into a pipeline. ergo's left-to-right direction
matches Node.js `stream.pipeline()`, RxJS `pipe()`, and lodash `_.flow()` —
all established left-to-right composition patterns.

The FP convention where `compose` means right-to-left (Ramda, Redux) is one
convention, not the only one. ergo's pipeline model — data flows through
ordered stages — is inherently left-to-right.

**Rejected:** `pipeline` (ambiguous with the architectural concept — ~300
occurrences as a concept word), `chain` (name collision with internal
utilities), `flow` (lodash-specific semantics without the two-accumulator
model).

## The Problem

Most Node.js frameworks treat middleware as a flat chain where anything can
happen in any order. This leads to common failure modes:

- **Wasted work**: body parsing and database queries execute before discovering
  the request is unauthorized.
- **Inconsistent errors**: each handler invents its own error format, making
  client-side error handling fragile.
- **Silent failures**: middleware swallows errors or returns ambiguous status
  codes, making debugging harder.
- **RFC drift**: teams start with good intentions but gradually diverge from
  HTTP semantics because the framework doesn't enforce them.

## The Solution: Structured Fast Fail

ergo organizes every request through four ordered stages:

```
Request → Negotiation → Authorization → Validation → Execution → Response
              ↓               ↓              ↓
          406/415          401/403          400/422
```

Each stage either succeeds (passes the request forward) or fails (returns an
[RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) Problem Details error response). Failures are _immediate_ — no
downstream middleware executes.

### Why This Matters

**Fail Fast** is a well-established principle in systems design. The concept
originates from Jim Shore's influential essay
["Fail Fast"](https://www.martinfowler.com/ieeeSoftware/failFast.pdf)
(IEEE Software, 2004), which argues that bugs are easier to find when software
fails immediately and visibly rather than producing incorrect results silently.

In the HTTP context, this principle maps directly to [RFC 9110](https://www.rfc-editor.org/rfc/rfc9110)'s status code
semantics: a `401 Unauthorized` should be returned _before_ attempting to parse
a request body, and a `406 Not Acceptable` should be returned _before_
executing business logic. ergo makes this the only possible execution path.

### Standards as Guardrails

ergo doesn't just _support_ RFCs — it enforces them structurally:

| Concern | Standard | ergo Behavior |
| ------- | -------- | ------------- |
| Error responses | [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) | All errors are Problem Details JSON |
| Content negotiation | [RFC 9110 §12.5](https://www.rfc-editor.org/rfc/rfc9110#section-12.5) | Automatic 406 for unacceptable media types |
| Authentication | [RFC 6750](https://www.rfc-editor.org/rfc/rfc6750), [RFC 7617](https://www.rfc-editor.org/rfc/rfc7617) | Bearer/Basic with proper WWW-Authenticate |
| Caching | [RFC 9111](https://www.rfc-editor.org/rfc/rfc9111) | Cache-Control and conditional request support |
| CORS | [Fetch Standard](https://fetch.spec.whatwg.org/#http-cors-protocol) | Preflight and simple request handling |
| CSRF | [OWASP Guidelines](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html) | Double-submit cookie pattern |
| Rate limiting | [RFC 6585 §4](https://www.rfc-editor.org/rfc/rfc6585#section-4) | 429 Too Many Requests with Retry-After |
| Security headers | [RFC 6797](https://www.rfc-editor.org/rfc/rfc6797) | HSTS, CSP, and security header defaults |

### Secure by Default

ergo doesn't treat security as an opt-in feature — it's a consequence of the
pipeline design and conservative defaults. You would have to deliberately
override these protections to weaken them:

- **Security headers** ship with restrictive defaults: `Content-Security-Policy:
  default-src 'none'`, `X-Frame-Options: DENY`, `X-Content-Type-Options:
  nosniff`, `X-XSS-Protection: 0` (disables flawed browser XSS filters).
- **Null-prototype objects** for all user-input-derived data (query params,
  cookies, Prefer headers) prevent prototype pollution attacks.
- **Bounded input parsing** limits query string length, key-value pair count,
  cookie count, request body size, and decompressed body size out of the box.
- **Decompression bomb protection** enforces a separate limit on inflated body
  size after decompression, preventing small compressed payloads from expanding
  to arbitrary sizes in memory.
- **Header injection prevention** via `sanitizeQuotedString` for all values
  interpolated into HTTP header quoted-strings (WWW-Authenticate, Link,
  Set-Cookie).
- **Timing-safe CSRF verification** uses `crypto.timingSafeEqual()` to prevent
  timing attacks on token comparison.
- **Sensitive header redaction** in the structured logger (`authorization`,
  `cookie`, `proxy-authorization`, `set-cookie`).
- **5xx error detail redaction** ensures internal error messages are never
  leaked to API consumers.

These protections satisfy the
[OWASP REST Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html)
recommendations and address multiple risks in the
[OWASP API Security Top 10](https://owasp.org/API-Security/). See the
[Security](/concepts/security/) concepts page for a detailed mapping.

### Further Reading

- Jim Shore, ["Fail Fast"](https://www.martinfowler.com/ieeeSoftware/failFast.pdf),
  IEEE Software, Sept/Oct 2004
- Mark Nottingham,
  ["Building Protocols with HTTP"](https://www.rfc-editor.org/rfc/rfc9205)
  ([RFC 9205](https://www.rfc-editor.org/rfc/rfc9205)) — best practices for HTTP-based APIs
- ["HTTP Semantics"](https://www.rfc-editor.org/rfc/rfc9110) ([RFC 9110](https://www.rfc-editor.org/rfc/rfc9110)) — the
  authoritative specification for HTTP methods, status codes, and headers
- [OWASP API Security Top 10](https://owasp.org/API-Security/) — the most
  critical API security risks
- [OWASP REST Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html) —
  practical REST API security guidance
